{
  "compilerOptions": {
    "declaration": true,       // Habilitar la generación de archivos .d.ts
    "declarationDir": "./dist", // Directorio donde se generarán los .d.ts
    "allowJs": true,            // Permitir archivos .js
    "esModuleInterop": true,    // Para compatibilidad con módulos ES6
    "skipLibCheck": true,       // Opcional: evita problemas con bibliotecas
    "outDir": "./dist",         // Asegura que todo se compila a dist
    "moduleResolution": "node", // Asegura la resolución de módulos de la forma que Node.js lo hace
    "target": "ES6"             // Puedes ajustarlo a la versión de JS que necesites
  },
  "include": [
    "src/**/*.ts",  // Asegúrate de incluir todos los archivos TS
    "src/**/*.d.ts"  // Asegúrate de incluir todos los archivos TS
  ]
}